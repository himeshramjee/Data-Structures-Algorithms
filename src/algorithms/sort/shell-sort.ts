import { NumberSort } from "./number-sort"

// Using Knuths Iterval Squence h=1,h = 3h + 1, implement an n-sort function
class ShellSort extends NumberSort {

  sort() {
    console.log("TODO: Using Knuths Iterval Squence h = 3h + 1, implement an n-sort (shell sort) function");
  }
}

const ss = new ShellSort(100);
ss.sortItems();